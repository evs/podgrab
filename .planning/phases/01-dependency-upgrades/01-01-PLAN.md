---
phase: 01-dependency-upgrades
plan: 01
name: Go Version Upgrade + ioutil Replacement
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - service/podcastService.go
  - service/fileService.go
requirements:
  - DEPS-01
  - DEPS-08
autonomous: true

must_haves:
  truths:
    - "go build compiles successfully with Go 1.24+"
    - "No io/ioutil imports remain in any .go file"
    - "App starts and serves HTTP requests after the upgrade"
  artifacts:
    - path: "go.mod"
      provides: "Go 1.24 directive and module compatibility"
      contains: "go 1.24"
    - path: "service/podcastService.go"
      provides: "HTTP response body reading without ioutil"
      contains: "io.ReadAll"
    - path: "service/fileService.go"
      provides: "File writing without ioutil"
      contains: "os.WriteFile"
  key_links:
    - from: "service/podcastService.go"
      to: "io.ReadAll"
      via: "replaces ioutil.ReadAll"
      pattern: "io\\.ReadAll"
    - from: "service/fileService.go"
      to: "os.WriteFile"
      via: "replaces ioutil.WriteFile"
      pattern: "os\\.WriteFile"
---

<objective>
Upgrade Go from 1.15 to 1.24+ and replace all deprecated io/ioutil usage.

Purpose: Establish a modern Go toolchain as the foundation for all subsequent dependency upgrades. Go 1.22+ removes io/ioutil entirely, so this must be done before any other changes. Isolating the Go version change per D-09.

Output: Working binary that compiles and runs on Go 1.24+ with no deprecated ioutil calls.
</objective>

<execution_context>
@/Users/rentamac/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rentamac/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-upgrades/01-CONTEXT.md
@.planning/phases/01-dependency-upgrades/01-RESEARCH.md
@.planning/research/PITFALLS.md

<interfaces>
From go.mod:
```go
module github.com/akhilrex/podgrab
go 1.15
// Direct dependencies: gin v1.7.2, gorm v1.20.2, gorilla/websocket v1.4.2, etc.
```

From service/podcastService.go (line 7):
```go
"io/ioutil"
```

From service/fileService.go (line 10):
```go
"io/ioutil"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Go version to 1.24+ and fix go.mod</name>
  <files>go.mod, go.sum</files>
  <read_first>go.mod, go.sum</read_first>
  <action>
1. Edit `go.mod`: change `go 1.15` to `go 1.24`
2. Run `go mod tidy` to update the module graph for the new Go version
3. If `go mod tidy` reports errors about `io/ioutil` deprecation, note them but do NOT fix them yet (that's Task 2)
4. If `go mod tidy` reports the `+incompatible` directive on `dgrijalva/jwt-go`, that's expected — it will be removed in Plan 02
5. Run `go build ./...` to verify compilation succeeds or identify remaining issues. If `ioutil` deprecation causes compile errors (Go 1.22+ removes the package entirely), those get fixed in Task 2
6. Run `go vet ./...` and note any new warnings that appear with Go 1.24
  </action>
  <acceptance_criteria>
    - `go.mod` contains the line `go 1.24` (not `go 1.15`)
    - `go mod tidy` exits with code 0 or only reports ioutil-related deprecation warnings
    - `go build ./...` compiles successfully OR only fails on `io/ioutil` import errors (to be fixed in Task 2)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rentamac/Dev/podgrab && grep '^go 1\.24' go.mod && go build ./...</automated>
  </verify>
  <done>go.mod specifies Go 1.24; go mod tidy completes; go build succeeds (after Task 2 fixes ioutil)</done>
</task>

<task type="auto">
  <name>Task 2: Replace all io/ioutil usage with modern equivalents</name>
  <files>service/podcastService.go, service/fileService.go</files>
  <read_first>service/podcastService.go, service/fileService.go</read_first>
  <action>
In `service/podcastService.go`:
1. Replace the import `"io/ioutil"` with nothing (remove it). The file likely also uses `io`, so ensure `io` import is present if needed by other code
2. Replace `ioutil.ReadAll(resp.Body)` on line ~726 with `io.ReadAll(resp.Body)`
3. Remove the `"io/ioutil"` import from the import block; ensure `"io"` is in the import block if not already present

In `service/fileService.go`:
1. Replace the import `"io/ioutil"` with nothing (remove it). The file likely also uses `os`, so ensure `os` import is present
2. Replace `ioutil.WriteFile(finalPath, []byte(toPersist), 0644)` on line ~103 with `os.WriteFile(finalPath, []byte(toPersist), 0644)`
3. Remove the `"io/ioutil"` import from the import block; ensure `"os"` is in the import block if not already present

After both files are updated:
4. Run `go build ./...` to verify the entire project compiles
5. Run `go vet ./...` to check for any new warnings
6. Run `grep -r "io/ioutil" --include="*.go" .` to verify zero remaining ioutil imports
  </action>
  <acceptance_criteria>
    - `service/podcastService.go` contains `io.ReadAll` and does NOT contain `ioutil.ReadAll`
    - `service/podcastService.go` does NOT import `"io/ioutil"`
    - `service/fileService.go` contains `os.WriteFile` and does NOT contain `ioutil.WriteFile`
    - `service/fileService.go` does NOT import `"io/ioutil"`
    - `grep -r "io/ioutil" --include="*.go" .` returns no matches
    - `go build ./...` exits with code 0
    - `go vet ./...` exits with code 0 (or only warnings unrelated to ioutil)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rentamac/Dev/podgrab && go build ./... && ! grep -r "io/ioutil" --include="*.go" . && grep "io\.ReadAll" service/podcastService.go && grep "os\.WriteFile" service/fileService.go</automated>
  </verify>
  <done>All io/ioutil usage replaced with io.ReadAll and os.WriteFile; go build succeeds; no ioutil imports remain anywhere in the codebase</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → Compiler | Go version change may surface latent bugs in compilation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | go.mod | accept | Go version is a build dependency, not a trust boundary |
| T-01-02 | T | ioutil replacement | accept | Replacement functions have identical behavior per Go stdlib docs |
</threat_model>

<verification>
1. `go build ./...` exits with code 0 — entire project compiles on Go 1.24+
2. `grep -r "io/ioutil" --include="*.go" .` returns no output — zero deprecated ioutil calls remain
3. `go vet ./...` exits with code 0 — no new static analysis warnings introduced
4. `go.mod` contains `go 1.24` — version directive updated
5. Start the app with `go run main.go` and verify it starts without errors (quick manual check or `curl localhost:8080` in a test environment)
</verification>

<success_criteria>
- Go 1.24+ is the target version in go.mod
- All `io/ioutil` calls replaced with `io.ReadAll` and `os.WriteFile`
- `go build ./...` succeeds
- `go vet ./...` passes
- No `io/ioutil` imports remain in any Go source file
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-upgrades/01-01-SUMMARY.md`
</output>